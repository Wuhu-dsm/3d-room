---
quick_id: 260524-0u7
slug: deskspritecan-zhangxuefeng-kadun-miaomia
status: planned
date: 2026-05-23
---

# Quick Task 260524-0u7: Desktop Props Iteration

Goal: remove the temporary `deskSpriteCan` prop, place the provided `zhangxuefeng.glb` Sprite prop behind the ice tea model, place `kadun.glb` as a clickable speaker to the left of the first monitor, and place `paint.glb` on the empty wall above the first monitor.

Plan:

1. Update the base resource manifest to remove `deskSpriteCan` and add `zhangxuefeng`, `kadun`, `paint`, and `miaomiao`.
2. Replace `DeskProps.setDeskSpriteCan()` with reusable GLB prop setup and add transforms for `zhangxuefeng`, `kadun`, and `paint`.
3. Add `kadunSpeaker` to raycast selection so hover outline/cursor works and click plays `miaomiao.mp3`.
4. Build and smoke-check that all assets load and the page still starts.
