---
quick_id: 260524-0u7
slug: deskspritecan-zhangxuefeng-kadun-miaomia
status: complete
date: 2026-05-23
---

# Quick Task 260524-0u7 Summary

Task: remove `deskSpriteCan`, add the `zhangxuefeng` Sprite prop behind the ice tea model, add clickable `kadun` speaker near the first monitor, and add `paint` wall art above the first monitor.

Completed:

- Removed `deskSpriteCan` from runtime resources and deleted its static GLB.
- Added `zhangxuefeng.glb`, `kadun.glb`, `paint.glb`, and `miaomiao.mp3` to the base resource manifest.
- Updated `DeskProps` to place:
  - `zhangxuefeng` behind/near the ice tea prop.
  - `kadunSpeaker` to the left side of the first monitor.
  - `paint` on the wall above the first monitor.
- Added `kadunSpeaker` to the raycast targets.
- Wired `kadunSpeaker` click to play `miaomiao.mp3` via the existing `AudioManager`.

Verification:

- `npm run build` passes.
- Browser smoke check on `http://localhost:8085` reached START, entered the room, and reported no console warnings/errors.
- Browser asset inventory confirmed `zhangxuefeng.glb`, `kadun.glb`, `paint.glb`, and `miaomiao.mp3` were requested.
- Browser asset inventory no longer showed `deskSpriteCan.glb`.

Notes:

- Placement uses current camera/room coordinates and may still benefit from visual micro-adjustment in a later polish pass.
- The repository had substantial pre-existing uncommitted changes; this quick task only layered this prop iteration on top.
